#pragma once

/*
 * Hexagon SDK 6.5.0.1 remote.h includes this Windows-only extension header,
 * but the Community Edition package used here does not ship it. cdsp_peak only
 * needs the standard FastRPC declarations that follow the include in remote.h.
 */
